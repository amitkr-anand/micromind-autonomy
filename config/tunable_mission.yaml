# MicroMind / NanoCorteX — Tunable Mission Parameters
# Classification: Programme Confidential
# Governance: §4 Config Governance (no hardcoded thresholds in production code)
# Authority: Deputy 1 (Agent 1)
# Created: Gate 3 — NAV-02 TRN ESKF injection

# ── TRN measurement noise ────────────────────────────────────────────────────
# Source: core/ekf/error_state_ekf.py ErrorStateEKF._R_TRN_NOMINAL
# Used by: ErrorStateEKF.update_trn() — R_eff = R_TRN_NOMINAL / clip(w, 0.1, 1.0)
trn_nominal_noise_horizontal_m: 5.0   # 1-sigma horizontal TRN noise (m)
                                       # Diagonal: [5^2, 5^2, 1e6] m²
                                       # 1e6 vertical — TRN provides no vertical

# ── NavigationManager thresholds ────────────────────────────────────────────
# Source: core/navigation/navigation_manager.py
vio_confidence_threshold: 0.50         # Minimum VIO confidence to inject into ESKF
nav_confidence_shm_threshold: 0.20     # Unified nav confidence below which SHM triggered
trn_interval_m: 5000.0                 # Distance between TRN correction attempts (m)

# ── FSM degraded state thresholds ───────────────────────────────────────────
# Source: core/state_machine/state_machine.py
nav_trn_only_vio_threshold: 0.15       # VIO confidence below which NAV_TRN_ONLY entered
nav_ins_only_trn_gap_km: 15.0          # km since last TRN before INS_ONLY declared
